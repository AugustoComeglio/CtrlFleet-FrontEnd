import { FiltrarRegistroCombustiblePipe } from './filtrar-registro-combustible.pipe';

describe('FiltrarRegistroCombustiblePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarRegistroCombustiblePipe();
    expect(pipe).toBeTruthy();
  });
});
